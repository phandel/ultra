.DS_Store
invite/

# launchd logs for the birthday-wishes photo sync -- never publish these
photos/*.log

# The raw Messages pull: FULL TEXT of private birthday texts, approved:false by default.
# It was tracked and therefore live at phandel.com/wishes/candidates.json -- anyone could
# read 15 friends' messages verbatim. Only wishes/published.json (approved, initials only)
# may ever ship. Keep this ignored.
wishes/candidates.json

# Stale Aug-21 draft of the invite page. Not for publication.
index-mockup.html
